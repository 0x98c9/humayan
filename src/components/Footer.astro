---
import { Github, Linkedin, Mail, Heart, ArrowUp } from 'lucide-astro';
---

<footer class="bg-slate-900 border-t border-slate-800 py-8 sm:py-12">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 sm:gap-8 mb-6 sm:mb-8 animate-on-scroll animate-fade-in-up">
      <!-- Brand & Description -->
      <div class="space-y-3 sm:space-y-4 sm:col-span-2 lg:col-span-1">
        <h3 class="text-lg sm:text-xl font-bold text-white">Humayan</h3>
        <p class="text-slate-400 text-sm leading-relaxed">
          Building educational tools and exploring new technologies. 
          Always learning, always growing.
        </p>
        <div class="flex items-center gap-2 text-xs sm:text-sm text-slate-500">
          <span>Made with</span>
          <Heart size={14} class="text-red-400 flex-shrink-0 sm:w-4 sm:h-4" />
          <span>using Astro & Tailwind CSS</span>
        </div>
      </div>
      
      <!-- Quick Links -->
      <div class="space-y-3 sm:space-y-4">
        <h4 class="text-base sm:text-lg font-semibold text-white">Quick Links</h4>
        <div class="space-y-1.5 sm:space-y-2">
          <a href="#about" class="block text-slate-400 hover:text-white transition-colors text-sm">About</a>
          <a href="#projects" class="block text-slate-400 hover:text-white transition-colors text-sm">Projects</a>
          <a href="#skills" class="block text-slate-400 hover:text-white transition-colors text-sm">Skills</a>
          <a href="#contact" class="block text-slate-400 hover:text-white transition-colors text-sm">Contact</a>
        </div>
      </div>
      
      <!-- Social Links -->
      <div class="space-y-3 sm:space-y-4">
        <h4 class="text-base sm:text-lg font-semibold text-white">Connect</h4>
        <div class="flex space-x-3 sm:space-x-4">
          <a 
            href="https://github.com/0x98c9" 
            target="_blank" 
            rel="noopener noreferrer"
            class="w-9 h-9 sm:w-10 sm:h-10 bg-slate-800 rounded-lg flex items-center justify-center text-slate-400 hover:text-white hover:bg-slate-700 transition-colors"
            aria-label="GitHub"
          >
            <Github size={18} class="sm:w-5 sm:h-5" />
          </a>
          <a 
            href="https://linkedin.com/in/humayanalrosid" 
            target="_blank" 
            rel="noopener noreferrer"
            class="w-9 h-9 sm:w-10 sm:h-10 bg-slate-800 rounded-lg flex items-center justify-center text-slate-400 hover:text-white hover:bg-slate-700 transition-colors"
            aria-label="LinkedIn"
          >
            <Linkedin size={18} class="sm:w-5 sm:h-5" />
          </a>
          <a 
            href="mailto:humayanalrosid3@gmail.com"
            class="w-9 h-9 sm:w-10 sm:h-10 bg-slate-800 rounded-lg flex items-center justify-center text-slate-400 hover:text-white hover:bg-slate-700 transition-colors"
            aria-label="Email"
          >
            <Mail size={18} class="sm:w-5 sm:h-5" />
          </a>
        </div>
      </div>
    </div>
    
    <!-- Bottom Section -->
    <div class="flex flex-col sm:flex-row items-center justify-between pt-6 sm:pt-8 border-t border-slate-800 gap-4 sm:gap-0">
      <div class="text-slate-500 text-xs sm:text-sm text-center sm:text-left">
        © 2025 Humayan. All rights reserved.
      </div>
      
      <!-- Back to Top -->
      <button 
        id="back-to-top"
        class="flex items-center gap-2 text-slate-400 hover:text-white transition-colors text-xs sm:text-sm group order-first sm:order-last"
        aria-label="Back to top"
      >
        <span>Back to top</span>
        <ArrowUp size={14} class="transition-transform group-hover:-translate-y-1 sm:w-4 sm:h-4" />
      </button>
    </div>
  </div>
</footer>

<script>
  // Back to top functionality
  const backToTopButton = document.getElementById('back-to-top');
  
  if (backToTopButton) {
    backToTopButton.addEventListener('click', () => {
      window.scrollTo({
        top: 0,
        behavior: 'smooth'
      });
    });
  }
  
  // Show/hide back to top button based on scroll position
  window.addEventListener('scroll', () => {
    if (backToTopButton) {
      if (window.pageYOffset > 500) {
        backToTopButton.style.opacity = '1';
      } else {
        backToTopButton.style.opacity = '0.5';
      }
    }
  });
</script>
